Someone reached out to the website

  Here are the details:

  * name: <%= "#{@contact_message.first_name} #{@contact_message.last_name}" %>  
  * email: <%= "#{@contact_message.email}" %> 
  * phone: <%= "#{number_to_phone(@contact_message.phone_number)}" %> 
  * subject: <%= "#{@contact_message.subject}" %>
  * message: <%= "#{@contact_message.message}" %>
  * created at: <%= @contact_message.created_at.strftime("%A, %d %b %Y %l:%M %p") %>
